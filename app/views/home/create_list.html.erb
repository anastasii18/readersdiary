<div class="lists">
  Cписок <%= @list.list_name %>!

  <div class="books">
    Книги:
    <% @list.books.each do |book| %>
      <div class="titles">
        <%= book.title %>
      </div>
    <% end %>
  </div>
</div>