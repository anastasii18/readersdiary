<div class="lists">
  Cписок <%= @list.list_name %>!

  <div class="lists">
    Книги:
    <% @list.books.each do |book| %>
      <div class="titles">
        <%= link_to book.title, "/about_book/#{book.id}"%>
      </div>
    <% end %>
  </div>
</div>