<div class="lists">
  Автор <%= @author.name %>!

  <div>
    <% @author.books.each do |book| %>
      <div class="titles">
        <%= link_to book.title , "/about_book/#{book.id}" %>
      </div>
    <% end %>
  </div>
</div>


